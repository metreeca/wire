{
  "private": true,
  "version": "0.10.0",
  "description": "Pluggable storage connectors for relational and graph databases.",
  "keywords": [
    "typescript",
    "database",
    "connector",
    "sql",
    "gql",
    "cypher",
    "sparql"
  ],
  "workspaces": [
    "packages/*/*"
  ],
  "scripts": {
    "clean": "npm run clean --workspaces && rm -rf dist docs node_modules",
    "prime": "npm ci",
    "setup": "npm ci && sh package.sh",
    "build": "npm run build --workspaces && rm -rf docs && npx typedoc",
    "check": "npm run check --workspaces",
    "proof": "npm run build && npx wds --root-dir docs --open"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@web/dev-server": "^1.0.0",
    "typedoc": "^0.28.18",
    "vitest": "^4.0.13"
  },
  "allowScripts": {
    "fsevents@2.3.3": true
  }
}
